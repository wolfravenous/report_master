<div id="unit">
  <header>
    <h1><%= @subject.name %></h1>
  </header>
  <header>
    <h2><%= @unit.title %></h2>

    <ul class="actions">
      <li><%= link_to "Edit Unit", [:edit, @subject, @unit],
        class: "edit" %></li>
    </ul>

  </header>

  <%= simple_format(@unit.content) %>
</div>
