<%= simple_form_for(report) do |f| %>

  <%= f.input :name %>
  <%= f.input :gender, as: :radio_buttons, collection: ['female', 'male'] %>



    <%= f.association :subject %>

    <%= f.input :intro_id, collection: Subject.all, as: :grouped_select, group_method: :intros %>

    <%= f.input :unit_id, collection: Subject.all, as: :grouped_select, group_method: :units %>

    <%= f.input :initial_id, collection: Subject.all, as: :grouped_select, group_method: :units %>

    <%= f.input :ending_id, collection: Subject.all, as: :grouped_select, group_method: :units %>

    <%= f.input :honor, label: "Honors?" %>
    <%= f.input :accel, label: "Accellerating?" %>
    <%= f.input :maxonline, label: "Maximize Scores for Online Assignments?" %>

    <%= f.input :future_id, collection: Subject.all, as: :grouped_select, group_method: :futures %>

  <%= f.input :classwork, label: "Classwork, 0 => NC, 1 => NA" %>
  <%= f.input :assessment, label: "Assessment, 0 => NC, 1 => NA"  %>
  <%= f.input :participate, label: "Participation, 0 => NC, 1 => NA"  %>
  <%= f.input :comment, label: 'Personal Comments' %>

  <%= f.button :submit, class: "btn-primary" %>
<% end %>
